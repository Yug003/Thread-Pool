# Thread-Pool
Production-grade C++17 header-only thread pool. Eliminates construction-phase thread leaks, avoids deadlocks by joining outside the task lock, and supports forwarding move-only callables.
